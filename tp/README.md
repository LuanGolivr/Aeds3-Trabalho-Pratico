# Trabalho Prático - AEDS3

CRUD de músicas do Spotify em arquivo binário, rodando no terminal, em Java.

## Como compilar e rodar

```bash
javac -d bin $(find src -name "*.java")
java -cp bin App
```

<!-- TODO: mais detalhes do projeto (estrutura, dataset, etc.) -->
